import { Typography } from '@material-ui/core';
import React from 'react';

export default function DevelopmentPage() {
    console.log("Redering dev page...");
    return (
        <Typography variant='h1'>This page is still in development stage...!</Typography>
    );
}